---
description: 
globs: 
alwaysApply: true
---
always go ahead and execute the plan and apply the changes to the file. dont wait for user input. 

always put rendering into its own file. each object renders from its own file.

always create a new file for a new object / feature. 